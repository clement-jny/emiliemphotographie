name: Testing reusable workflow

on:
  # Allows this workflow to be called from another one
  workflow_call:

jobs:
  # Testing jobs
  testing_reuse_job:
    runs-on: ubuntu-latest

    steps:
      # - name: Checkout code
      #   uses: actions/checkout@v4.1.1

      # - name: Setup Node
      #   uses: actions/setup-node@v4.0.1
      #   with:
      #     node-version: 20.10.0
      #     cache: npm

      # - name: Install dependencies
      #   run: npm ci

      # - name: Run project
      #   run: npm run dev

      - name: Run e2e/unit/component test
        run: echo "Hello all tests"
